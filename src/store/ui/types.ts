const prefix = 'UI';
const SET_FORM_STEP = `${prefix}.SET_FORM_STEP`;
const SET_ERROR = `${prefix}.SET_ERROR`;
const CLEAR_ERROR = `${prefix}.CLEAR_ERROR`;
const SET_ERROR_TYPE = `${prefix}.SET_ERROR_TYPE`;
const SET_CLEAR_UI_DATA = `${prefix}.SET_CLEAR_UI_DATA`;

export default {
  prefix,
  SET_FORM_STEP,
  SET_ERROR,
  CLEAR_ERROR,
  SET_ERROR_TYPE,
  SET_CLEAR_UI_DATA,
};
