import { all } from 'redux-saga/effects';
// import { saga as routingSaga } from './routing';
// import { saga as formDataSaga } from './formData';

export default function* rootSaga() {
  //   yield all([...routingSaga, ...formDataSaga]);
}
